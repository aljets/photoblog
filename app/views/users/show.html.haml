%h1
  = @user.name
  - if current_user.admin? || current_user?(@user)
    = link_to 'edit', edit_user_path(@user)
  - if current_user.admin? && !current_user?(@user)
    = link_to 'delete', @user, method: :delete,
                              data: { confirm: "You sure?" }
%h1 Images
%p Placeholder for finding all images by #{@user.name}
